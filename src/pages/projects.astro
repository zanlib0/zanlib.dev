---
import ProjectsPage from '../components/pages/ProjectsPage.astro';
import { PAGE_METADATA } from '../consts';
import { getNavLinks } from '../i18n/ui';

const t = {
	lang: 'en' as const,
	...PAGE_METADATA.en.projects,
	navLinks: getNavLinks('en'),
};
---

<ProjectsPage {t} />
