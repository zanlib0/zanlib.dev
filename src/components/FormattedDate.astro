---
interface Props {
	date: Date;
	language?: string;
}

const { date, language = 'en-uk' } = Astro.props;
---

<time datetime={date.toISOString()} class="date">
	{
		date.toLocaleDateString(language, {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
